// create a function generate random number when function calls.
// Math.trunc() method used for remove the digit after decimal.
// let number = () => Math.trunc(Math.random()*1000)
// console.log(number());

// Math.random() method  used for generating random value

// The Math.round() function returns the value of a number rounded to the nearest integer.
//let number = () => Math.round(Math.random()*1000)
// console.log(number());


//The Math.ceil() function returns the smallest integer greater than or equal to a given number.
// let number = () => Math.ceil(Math.random()*1000)
// console.log(number());

// The Math.floor() function returns the largest integer less than or equal to a given number.
let number = () => Math.floor(Math.random()*1000)
console.log(number());
